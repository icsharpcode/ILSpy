<UserControl x:Class="ICSharpCode.ILSpy.Analyzers.AnalyzerPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ICSharpCode.ILSpy.Analyzers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" Visibility="{Binding FilterVisibility}">
			<TextBlock Text="" VerticalAlignment="Center" Margin="3,0,0,0">
				<Bold>
					<Run Text="Namespace: " />
				</Bold>
				<Run Text="{Binding InNamespaceFilter, FallbackValue=&lt;all&gt;}" />
			</TextBlock>
			<TextBlock Text="" VerticalAlignment="Center">
				<Run Text=", " />
				<Bold>
					<Run Text="Assembly: " />
				</Bold>
				<Run Text="{Binding InAssemblyFilterText, Mode=OneWay}" />
			</TextBlock>
			<Button Content="Reset Filter" Command="{Binding ResetFilter}" Margin="3" />
		</StackPanel>
		<local:AnalyzerTreeView x:FieldModifier="public" x:Name="TreeView" Grid.Row="1" />
	</Grid>
</UserControl>
